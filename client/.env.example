# URL of the Express backend.
# Leave empty (or unset) for local dev — the Vite proxy handles /api requests.
# Set to your deployed backend URL for production builds.
VITE_API_URL=https://your-backend.onrender.com
